# 05 · Spark SQL

## Objetivo

Resolver el mismo problema de dos formas: DataFrame API y SQL puro.

## Dataset

Reutilizar cualquiera de los datasets anteriores.

## Qué practico

- `createOrReplaceTempView()`
- `spark.sql(...)`
- comparar el mismo resultado con `df.groupBy()` vs `SELECT ... GROUP BY`

## Tiempo estimado

Una tarde.

## Resultado esperado

Las mismas métricas del proyecto 01 o 03, resueltas primero con DataFrame API y luego con Spark SQL, comparando legibilidad y plan de ejecución (`explain()`).

## Checklist de conceptos

- [ ] temp views
- [ ] spark.sql
- [ ] comparar con DataFrame API
- [ ] explain()

## Lecciones aprendidas

_(rellenar al terminar el proyecto)_
